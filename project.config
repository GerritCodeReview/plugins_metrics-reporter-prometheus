[access]
	inheritFrom = Public-Plugins
[submit]
	action = inherit
[project]
	description = This plugin exposes Gerrit metrics to Prometheus (https://prometheus.io/).
[access "refs/*"]
	owner = group plugins-metrics-reporter-prometheus
